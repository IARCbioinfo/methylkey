<?xml version="1.0"?>
<tool id="dmrs_plot2" name="Plot DMRs" version="0.0.1">
<description>plot DMRs</description>

<command>
	#import re
	#set inputDir=$re.sub("\.dat","_files",str($input))
	#set outputDir=$re.sub("\.dat","_files/",str($html))

	Rscript $__tool_directory__/../R/dmrs_plot.r 
	--meth $inputDir/meth.rdata
	--max $max
	
	#if $dmrs != ""
	--dmrs $dmrs
	#end if
	--output $html.files_path;
	
	mv $html.files_path/* $inputDir/. ;
	cp $inputDir/index.html $input;
	mkdir $outputDir;
	ln -sfvn $inputDir/* $outputDir/.;
	cp $input $html; 
</command>

<inputs>
	<param name="input" type="data" format="html" label="DMPs report" />
	<param name="max" type="text" value="10" label="Max dmps to plot" />
	<param name="dmrs" type="text" value="" label="dmrs to plot" /> 
</inputs>

<outputs>
	<data name="html" format="html" label="DMRs plots" />
</outputs>

<stdio>

<regex match="Warning message:"
           source="both"
           level="warning"
           description="Warning message:" />
    <regex match="error"
           source="both"
           level="fatal"
           description="error encountered" />
</stdio>


<help>

</help>

</tool>
