% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report_cache.R
\name{rebuild_report_step}
\alias{rebuild_report_step}
\title{Rebuild a Specific Report Step}
\usage{
rebuild_report_step(report, step, force = FALSE, render = FALSE)
}
\arguments{
\item{report}{A \code{MethylkeyReport} object.}

\item{step}{Character. Name of the step to rebuild
(e.g., "data_loading", "qc", "group_control", "model_analysis").}

\item{force}{Logical. If TRUE, remove existing output and force rerun (default: FALSE).}

\item{render}{Logical. If TRUE, render the step after rebuilding (default: FALSE).}
}
\value{
Invisibly returns the report object.
}
\description{
Force rerun of a specific analysis step without recalculating other steps.
Clears cache for the target step and optionally renders.
}
\details{
This function:
\enumerate{
\item Validates that the step exists in the report
\item Clears Quarto cache for the step
\item Optionally re-renders the step
}

Use when:
\itemize{
\item A step failed and you want to try again
\item You want to test parameter changes for one step
\item Previous rendering had errors
}
}
\examples{
\dontrun{
report <- create_methylkey_report(...) |>
  add_report_data_loading(...) |>
  add_report_qc(...)

# If QC step had issues, rebuild it
report <- rebuild_report_step(report, step = "qc", force = TRUE)
}
}
